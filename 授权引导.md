# VelaMux 移植包授权引导

本指南只帮助定位文件和操作系统设置，不修改 VelaMux。本次另一台 Mac 的实测结论：Host、Python 和 ChatGPT App 都涉及授权；此前遗漏 ChatGPT App，补授权后用户确认问题解决。其他 Agent 的权限归属可能不同，按实际客户端、系统提示与实测核对，不把它理解为任何场景都要给所有程序开启所有权限。

## 一、先区分两种弹窗

- “Apple 无法验证是否包含恶意软件”：这是首次运行的 Gatekeeper 放行。Python 和 Host 可能分别出现。
- 辅助功能、屏幕录制：这是本次实测所需的运行后隐私权限。放行程序并不等于这些权限已经开启。

遇到前一种弹窗，先点“完成 / 完了”，不要点“移到废纸篓 / ゴミ箱に入れる”。在系统设置 → 隐私与安全性（プライバシーとセキュリティ）找到对应程序的提示，选择“仍要打开 / このまま開く”，按系统提示确认。若安装器窗口正在等待，直接回到该窗口按回车重试，不必重开安装器。只有 `.command` 自身还未获准启动时，才需要放行后重新打开它。

包内 Python 启动失败时，交互式安装器会保留窗口，等待你放行后回车重试（q 取消）；不自动重跑已执行的安装步骤。若非交互命令退出 137 且系统同时显示 python3.12 被阻止，先完成这一步。不要先重装、修改程序、关闭 Gatekeeper，或把退出 137 单独认定为同一个原因。

安装器在正式目录完成文件复制后，还会检查安装目录的 Python 并运行 Host 的 `--post-event-access-status`。该查询不会开启控制会话或输入操作。返回 `granted` 或 `denied` 都代表 Host 能启动；`denied` 时仍须后续完成相应权限。被系统拦截、启动超时或其他错误都会显示具体原因，允许人工处理后重试，不把所有失败等同于权限未开。无交互终端时不等待，会返回错误并走安装回滚。

## 二、安装后授权要选哪些文件

**选择安装目录中的文件，不要选 Downloads 或解压目录中的副本，也不要选系统或 Homebrew 的另一个 Python。**

| 对象 | 在文件选择器中可粘贴的路径 |
|---|---|
| Python 真正的可执行文件 | `~/Library/Application Support/VelaMuxPortable/python/bin/python3.12` |
| VelaMux Host | `~/Library/Application Support/VelaMuxPortable/app/bin/VelaMuxMCPHost` |
| 客户端 App | 在“应用程序”中选实际使用的 ChatGPT.app 或 Codex.app；安装位置和名称以目标电脑为准 |

安装前目录尚不存在时，先完成包内 Python 的首次放行并安装。包内 Python 位于解压目录的 payload/python/bin/python3.12；这与安装后的授权对象是两件事。

### 最方便：让 Finder 选中 Python

在目标 Mac 终端复制下面命令，或让那台 Codex 执行。它只定位文件：

```sh
open -R "$HOME/Library/Application Support/VelaMuxPortable/python/bin/python3.12"
```

定位 Host：

```sh
open -R "$HOME/Library/Application Support/VelaMuxPortable/app/bin/VelaMuxMCPHost"
```

然后将 Finder 里选中的文件拖入对应权限列表（如果该页面接受拖入），再打开开关。

### 不能拖入时：用“＋”添加

1. 在对应权限设置页点击“＋”。
2. 在弹出的文件选择器里按 **⌘⇧G**（前往文件夹）。
3. 粘贴上表中以 `~/Library/` 开头的路径；不要加引号，也不要在这个框里写 `$HOME`。
4. 回车定位，选中对应文件并点击“打开 / 開く”，然后开启开关。若定位到目录，选择里面的 python3.12 或 VelaMuxMCPHost。
5. 客户端 App 从“应用程序”选择实际运行的那一个，不要选网页快捷方式或另一个同名副本。

若没有“＋”或不接受添加，先尝试实际的 bind/capture 让系统产生对应请求，再按系统提示处理；不要编辑系统权限数据库。

## 三、检查哪些权限页

进入 系统设置 → 隐私与安全性：

| 中文 | 日文 | 本项目中的作用 |
|---|---|---|
| 辅助功能 | アクセシビリティ | 控制与事件投递相关 |
| 屏幕与系统音频录制（或屏幕录制） | 画面収録とシステムオーディオ録音（或画面収録） | 截图相关 |

本次另一台 Mac 实测只开启辅助功能和屏幕录制，两处都有 ChatGPT、VelaMuxHost、Python 3.12，并未额外开启输入监控。输入监控不是本包统一要求；只有系统明确请求或具体诊断证据指向它时再核对。

逐页核对 Host、Python 和当前客户端 App 的相关条目与系统请求。特别注意：本次第二台 Mac 使用 ChatGPT App 启动本地 MCP，漏掉 ChatGPT 的授权会影响操作；不能因为 Host 的开关已开就跳过客户端。

某个组件未显示时，用上一节的准确路径定位。若系统要求密码、Touch ID 或“退出并重新打开”，由你确认。界面名称可能随系统语言或版本稍有不同。

只需按任务需要处理上述相关权限；本流程不要求给整个磁盘开放访问，也不要求关闭 SIP。

## 四、授权后重新验证

1. 按系统要求退出并重新打开实际客户端 App，先保存未完成的工作。
2. 让 VelaMux 用 quit 结束旧 Host，再通过下一次调用启动新 Host；不要用笼统的 pkill 停掉隧道或其他程序。
3. 确认调用的是这台 Mac 的本地连接 velamux_portable，不是指向另一台 Mac 的云端 VelaMux 插件。
4. list → bind → capture。先对普通窗口验证，再对目标应用验证。看到正确窗口的真实截图才算截图通过；之后由你授权点击或输入测试。

辅助检查命令：

```sh
"$HOME/Library/Application Support/VelaMuxPortable/app/bin/VelaMuxMCPHost" --post-event-access-status
```

它输出 granted 只表示事件投递这一项，不证明屏幕录制等其他权限已经可用。

## 五、遇到问题时先查什么

- `event tap installation failed`：先核对启动链的辅助功能授权和实际系统提示，并按要求重启；只有明确证据需要时再检查输入监控，不要求所有用户预先开启。不要直接改焦点代码。
- `imageCreationFailed`：先核对实际客户端、Python、Host 的屏幕录制授权，再重新截图；这个错误本身不能说明某应用禁止截图。
- 服务 ready，但不能 bind/capture：ready 仅说明 MCP 连接层正常，不是完整 GUI 验收。
- 本地地址访问失败但服务在运行：受限 Agent 环境可能阻止 localhost，先核对其网络权限，或用已配置的 stdio MCP 检查，不要据此直接重装。
- Finder 找不到文件：先确认安装已完成，目录叫 VelaMuxPortable，不是原设备的 VelaMux。

已安装的机器只需阅读这份新说明并补齐授权；**不必为这次文档更新重新安装或替换程序。**

## Apple 官方参考

- 首次打开与系统放行：https://support.apple.com/en-gb/102445
- 屏幕与系统音频录制权限：https://support.apple.com/en-ae/guide/mac-help/mchld6aa7d23/mac

具体三个进程的授权结论来自本次用户实测，不是上述 Apple 文档对所有启动方式的统一要求。
