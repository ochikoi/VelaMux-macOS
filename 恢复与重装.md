# 安装失败恢复与同版本重装

本工具只处理 VelaMuxPortable，不提供升级或降级。

## 先做的事

先让移植版调用 `velamux_quit`，再断开使用它的本地 stdio MCP 连接（必要时退出该客户端）。不要退出原来的其他 VelaMux 或隧道。安装器发现移植版 Host 或独立 stdio 客户端仍在运行时会停止，不强杀进程。

## 如何选择

- **首次安装失败，固定安装目录不存在**：处理显示的具体问题后，重新选择原来的安装选项。新安装器会把未成功的文件保留到 `.staging-*` 或 `.failed-*` 目录，而不占住正式安装目录。
- **旧安装器留下 VelaMuxPortable 目录，但没有完成安装**：选择菜单 4“失败恢复”，或 `./install.sh --recover`。程序会核对旧安装器的运行脚本、Host 哈希和设置，只接管能够明确识别的残留。不认识的目录、损坏的配置、只剩半套云端凭据会停止并保留现场。
- **已安装但程序文件损坏，希望重装同一版本**：选择菜单 5，或 `./install.sh --reinstall`。保留端口、key、隧道配置、客户端配置片段及其他现有文件，重置 app/python/tunnel 和启动脚本。Host 版本不同或新回执记录的 payload 身份不同则拒绝，不把重装当升级。
- **只想增加云端连接**：使用菜单 3，不要重新安装。追加云端不会重启已有本地 MCP，失败时撤销本次新增的云端配置。

旧回执只记录 Host 哈希，无法证明完整历史 payload 身份；首次从旧包维护时依赖该 Host 身份和安装目录记录。维护成功会记录完整 payload 身份，后续必须使用同一 payload 的包。

## 保留、回滚和诊断

正式替换前先在相邻目录准备文件。重装成功后，旧目录保存为：

`~/Library/Application Support/VelaMuxPortable.backup-时间-随机值/`

若本次启动或本地 ready 检查失败，会恢复旧目录和原启动项，并尝试恢复原本运行的服务。失败的新目录保留为 `.failed-*`，供诊断；不会自动删除旧备份。复制阶段失败时，原目录不动。

备份、staging 和 failed 目录可能包含 key；不要上传、发送或把它们打进发行包。确认系统权限和实际截图可用后，再自行决定是否删除这些备份。

掉电、强制终止（例如 kill -9）或磁盘故障可能阻止回滚代码执行。若正式目录不见但旁边有 backup，先停止操作并核对目录，不要直接创建一套新配置覆盖现场。此版本没有自动处理任意崩溃残留的升级/恢复守护程序。

## 手动停止／恢复移植版服务

先 quit Host 并断开对应客户端，再停止（本地模式没有 tunnel 服务，命令报找不到该服务可忽略）：

```sh
launchctl bootout "gui/$(id -u)/io.velamux.portable.tunnel"
launchctl bootout "gui/$(id -u)/io.velamux.portable.mcp"
```

确认仍使用原目录时，可重新加载存在的启动项：

```sh
launchctl bootstrap "gui/$(id -u)" "$HOME/Library/LaunchAgents/io.velamux.portable.mcp.plist"
# 仅云端模式再执行：
launchctl bootstrap "gui/$(id -u)" "$HOME/Library/LaunchAgents/io.velamux.portable.tunnel.plist"
```

需要恢复备份时，只选择自己确认的那一个 backup；先将失败的正式目录改名留存，再把该 backup 改回 `VelaMuxPortable`，不要直接合并目录。启动项中的绝对路径应仍指向正式目录。无法确认目录归属时保留现场，不运行删除命令。

## 常见失败

- Python 或 Host 被系统阻止：保持安装终端打开，按 `授权引导.md` 允许运行，再回到同一窗口按回车重试。输入 q 才取消；已经替换目录时会走回滚。退出 137 本身不等于确定的权限问题。
- 端口被占用：首次安装更换端口；维护时保留端口，排查占用者，不强杀其他服务。
- launchctl 拒绝启动：检查是否在登录用户的 GUI 会话中运行，不要 sudo；查看日志与 Gatekeeper。
- 云端 401/403／隧道不出现：不通过重装解决；核对 Runtime key、组织、工作区关联和权限。
- ready 正常但截图失败：补齐准确启动链的屏幕录制授权，按系统要求重启客户端后再验证。

无需修改 Host、MCP 代码或系统权限数据库。
