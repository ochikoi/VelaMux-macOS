# 云端连接指南

本地 MCP 不需要云端账号、API key 或隧道。没有所需权限，先选仅本地安装即可。

## 准备账号和隧道

以下入口与权限依据 OpenAI 官方 Secure MCP Tunnel 文档（2026-09-20 核对）：
https://developers.openai.com/api/docs/guides/secure-mcp-tunnels

1. 登录 https://platform.openai.com/settings/organization/tunnels ，选择实际要使用的组织，创建隧道并复制其 ID。创建需 Tunnels Read + Manage；运行和选择隧道需 Read + Use。没有入口或权限时由组织管理员处理。
2. 在 Platform 的 API key 设置中为相同组织准备 Runtime API key；管理隧道的 Admin key 与运行客户端所用 key 不同。不要将 Admin key、第三方中转站 key 或别人的 key 填进安装器。
3. 把隧道关联到要使用的 ChatGPT 工作区以及相关 Platform 组织。工作区的开发者模式权限与 Platform 隧道权限分开。
4. 为另一台同时使用的 Mac 准备独立隧道，避免把两个实例误接到一起。

## 在这台 Mac 安装

双击“安装 VelaMux.command”，选“本地＋云端”；已有本地安装选“添加云端”。按提示输入 Tunnel ID 和隐藏的 Runtime key。安装器保存 key 到安装目录 `runtime.key`（600 权限），不会把 key 写进 launchctl 命令行。不自动操作账号或创建隧道。

## 在 ChatGPT 连接

按官方指南，在开发者模式的插件创建界面选择 Tunnel，选中该隧道或填入 ID，再连接并核对工具。看不到隧道时先检查工作区关联和 Read + Use 权限。隧道客户端须持续运行。

本 ZIP 是供用户自行安装的桌面工具包，不等于公开插件商店上架；官方文档明确隧道用于私有 MCP 连接，不支持直接以该隧道做公开插件提交。

## 验证与排错

先用 README 中的 `runtime_launch.py check` 查看本地 MCP 与 Tunnel 状态，再进行实际 list、bind、capture。MCP ready 不能代表云端已经认证；401/403 优先检查 key 和账号权限，不要反复重装。遇到客户端显示旧工具说明时，先核对服务是否已加载新说明，再重连客户端。

不同账号套餐、工作区策略和产品界面可能不同；以实际权限和上述官方文档为准。本安装器不会绕过这些限制，也不会把本地 MCP 端口开放到公网。
