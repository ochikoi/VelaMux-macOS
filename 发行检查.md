# 发行检查（2026-09-20）

本次仅修改安装入口、安装脚本、说明与发行材料，不修改 payload 内的 Host、MCP、Python、tunnel-client 或资源。没有部署到本机、没有停止现有服务。

## 已完成

- 双击 `.command` 选择安装模式，无需手工拼接命令。
- 本地／本地＋云端／追加云端、失败恢复、同版本保留配置重装；不提供升级。
- 维护前检查移植版目录和服务归属；保留原配置和旧目录，启动失败回滚。
- 最新工具说明保持不变；权限说明按实测以辅助功能和屏幕录制为主。
- 云端入口、权限条件和私有连接用途按官方文档补充。
- 原作者 ochikoi 的闭源阶段商业使用、修改、再分发及署名许可。
- 收集 tunnel-client 对应提交的 LICENSE/NOTICE、运行时自带 Python LICENSE，以及 Tcl/Tk/Itcl/Thread 上游条款。

## 公开上传前仍需明确

1. **第三方完整依赖与发行来源**：见 THIRD-PARTY-NOTICES.md。当前收集到的顶层许可不能代替打包 Python 和 tunnel-client 的完整依赖审查；不能将此包标成“所有再分发材料已审核完毕”。
2. **资源权利**：确认虚拟鼠标等资源的来源与可分发权利。
3. **正式发布来源**：作者主页已为 https://github.com/ochikoi 。具体仓库尚未提供，发布时补上真实仓库/Release 链接，不虚构地址。
4. **新安装器另一台 Mac 验收**：此轮做隔离安装逻辑测试、语法/完整性检查，未在另一台机器上运行新安装器；Gatekeeper、系统权限与真实 GUI 控制仍需实际验收。

## 明确保留的限制

- Host 仍为原来的 ad-hoc 签名构建，未签名公证升级；可能需要系统手动放行。
- Host 内的原机编译路径未清理（用户尚未要求改动）；不是已发现的动态库运行依赖。
- 没有升级功能；不宣称有限测试证明所有应用/系统版本兼容。
- 安装目录备份可能含 key，发行时只发布干净的安装包 ZIP，禁止打包安装后的 VelaMuxPortable 目录。
- 软件发布包与 ChatGPT 插件商店上架是不同事项。
